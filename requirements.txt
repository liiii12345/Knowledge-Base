# 本项目零第三方依赖，全部使用 Python 标准库。
#
# 之所以坚持零依赖：
#   1. 定时任务环境（公司内网机器）装包受限，标准库保证一定能跑
#   2. 依赖越少，两年后重跑失败的概率越低
#
# 如需替换实现，以下包可选（脚本均不强制）：
#   PyYAML>=6.0        # 替代内置轻量 YAML 解析
#   requests>=2.31.0   # 替代 urllib
#   feedparser>=6.0.10 # 简化 RSS 解析
