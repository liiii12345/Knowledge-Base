# 本项目零第三方依赖，全部使用 Python 标准库。
#
# 需要联网抓取真实信源时：无需安装任何包（urllib + xml.etree 已足够）
# 需要调用大模型做抽取时：也无需 SDK，走 OpenAI 兼容的 HTTP 接口
#
# 如果你偏好用现成库，可按需安装（脚本会自动回退，不强依赖）：
#   requests>=2.31.0
#   feedparser>=6.0.10
#   PyYAML>=6.0
